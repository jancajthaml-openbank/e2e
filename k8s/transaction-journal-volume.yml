kind: PersistentVolume
apiVersion: v1
metadata:
  name: transaction-journal-volume
  namespace: openbank
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 4Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/transaction/journal"
